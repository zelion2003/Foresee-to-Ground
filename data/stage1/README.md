# Stage-1 Clips

Stage-1 training expects short video clips under `data/stage1/clips/`.

Use `tools/build_stage1_clips.py` to generate these clips from the preprocessed source videos.
Use `tools/check_stage1_clips.py` to filter clips into accepted and rejected sets when needed.
